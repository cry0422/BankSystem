package banksystem;

// an interface to generate random characters like letters or numbers
public interface RandomCharacters {
    public String randomLetters(int number);
    public long randomNumbers(int number);
}
